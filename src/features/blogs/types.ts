export type CreateBlogDto = {
    name: string;
    description: string;
    websiteUrl: string;
};

export type BlogType = {
    id: string;
    name: string;
    description: string;
    websiteUrl: string;
};
