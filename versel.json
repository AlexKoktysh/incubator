{
    "version": 2,
    "name": "incubator-hometask-1",
    "builds": [{ "src": "src/index.ts", "use": "@vercel/node" }],
    "routes": [{ "src": "/(.*)", "dest": "src/index.ts" }],
    "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
